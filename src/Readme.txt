# Spring Boot User Management API

## 📌 Setup Instructions
1. Clone the repository.
2. Setup MySQL and create the database `userdb`.
3. Run the `schema.sql` and `data.sql` in MySQL.
4. Start the Spring Boot application.
5. Use Postman to test the API.

## 📌 API Endpoints
- `POST /api/users/create_user`
- `POST /api/users/get_users`
- `POST /api/users/delete_user`
- `POST /api/users/update_user`

## 📌 Database Schema
See `db/schema.sql`.

## 📌 Test Data
See `db/data.sql`.
